syntax = "proto3";
package increment;

message EchoRequest {
    string message = 1;
}

message EchoResponse {
    string message = 1;
}

service EchoService {
    rpc EchoMessage (EchoRequest) 
        returns (EchoResponse);
}